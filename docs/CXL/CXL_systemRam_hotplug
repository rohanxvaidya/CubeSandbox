以下是为您整理的技术文档，针对 **CXL 内存分配逻辑、启动顺序影响及优化方案** 进行了系统化排版，方便您直接复制保存作为技术参考。

---

# CXL 异构内存架构优化技术手册 (2026版)

## 一、 核心背景：CXL 代码段分配的影响

在 CXL (DDR4) 与 Local (DDR5) 混插的环境下，物理延迟差异（~100ns-200ns）及带宽差异使得内存放置策略至关重要。

* **代码段敏感度：** 指令流（Instruction Stream）对延迟极度敏感。若代码段掉入 CXL，将导致前端取指（Instruction Fetch）停顿、分支预测失败后的恢复开销（Recovery Penalty）激增。
* **影响：** 典型指令密集型应用（如数据库、复杂后端逻辑）的 IPC（每时钟周期指令数）可能下降 **20% - 50%**。

---

## 二、 方案比较：自动在线化 vs. 用户态延迟激活

### 1. 内核自动在线化 (Default Mode)

现代内核（v6.x+）通过解析 ACPI (CEDT/SRAT) 表，在引导阶段自动将 CXL 配置为 `system-ram`。

| 特性 | 技术细节 |
| --- | --- |
| **触发时机** | 内核初始化阶段（早于 `systemd` / PID 1）。 |
| **防护机制** | 利用 `ZONE_MOVABLE` 机制。内核关键数据结构和不可移动页被禁止分配到 CXL 节点。 |
| **风险点** | 早期启动的第三方高优先级用户态服务可能在 Local 压力大时误入 CXL。 |
| **配置参数** | 在 GRUB 中添加 `movable_node` 确保 CXL 节点仅处理可移动页面。 |

### 2. 用户态延迟激活 (User-Controlled Mode)

通过干预驱动加载逻辑，使 CXL 在系统完全启动后才表现为可用内存。

| 特性 | 技术细节 |
| --- | --- |
| **工作原理** | 内核启动时仅保持 CXL 为 `/dev/dax` 设备，不计入系统总内存。 |
| **核心优势** | **100% 确定性**。确保所有系统核心服务（PID 1、日志、网络栈）完全驻留在 Local DDR5。 |
| **操作流程** | 系统就绪 -> 脚本检测 -> `daxctl` 热插拔在线化。 |

---

## 三、 实施指南：如何实现用户态手动配置

若要规避 `systemd` 及其核心服务占用 CXL 空间，请参考以下操作步骤：

### 1. 阻止内核自动在线化

在 GRUB 配置文件（如 `/etc/default/grub`）中，确保没有强制在线化的参数，或者在 BIOS 中将 CXL 拓扑设置为 **Device-DAX** 模式。

### 2. 检查设备状态

系统启动后，确认 CXL 内存未被识别为 RAM：

```bash
# 查看 NUMA 节点，确认此时只有 Node 0 (Local)
numactl -H

# 确认 CXL 表现为 DAX 设备
daxctl list -D

```

### 3. 执行手动转换脚本

编写一个延迟启动脚本（或在业务部署前手动执行），将设备转换为 `system-ram` 模式：

```bash
#!/bin/bash
# 检查是否存在名为 dax0.0 的 CXL 设备并将其上线
DEVICE="dax0.0"

if daxctl list -d $DEVICE | grep -q "devdax"; then
    echo "Detected CXL device $DEVICE in devdax mode. Reconfiguring..."
    # 强制将模式切换为 system-ram，这将动态创建一个新的 NUMA Node
    sudo daxctl reconfigure-device --mode=system-ram --force $DEVICE
    echo "CXL device $DEVICE is now online as System-RAM."
else
    echo "CXL device $DEVICE is already online or not found."
fi

```

---

## 四、 TPP (Transparent Page Placement) 机制

在 CXL 节点上线后，建议开启内核 TPP 功能以实现动态优化。

* **Proactive Demotion（主动降级）：** 当 Local DDR5 压力大时，自动将冷页移至 CXL。
* **Reactive Promotion（反应式晋升）：** 当 CXL 中的页再次变热（如代码段被频繁调用），立即移回 Local。
* **开启指令：**
```bash
# 启用层级内存平衡
echo 3 > /proc/sys/kernel/numa_balancing

```



---

## 五、 总结建议

1. **开发/专利测试阶段：** 建议使用 **用户态延迟激活**，确保系统底噪（System Noise）全部留在本地内存，使实验数据更纯净。
2. **生产环境部署：** 建议使用 **内核自动模式 + `movable_node` 参数 + TPP**，兼顾系统稳定性和自动化的冷热动态平衡。

---

> **保存提示：** 本文档涵盖了从底层固件到内核驱动，再到用户态管理的完整路径。建议将其存为 `.md` 文件或技术 Wiki 页面以便后续调优参考。





##### 如果是这种启动顺序，那么systemD本身这个一号进程可能会占用CXL node的内存，这种有什么办法规避？ 或者我们考虑一下cxl memory配置成system ram的方式有系统启动之后由用户自己配置，kernel不要去做，是否可行？



从系统设计与内存分配原理来看，你的顾虑是非常准确的。如果 CXL 内存由内核在系统启动早期自动在线化（Online），由于 `systemd`（PID 1）以及它早期拉起的各种核心服务（如 `udevd`、`journald` 等）在申请内存时，内核可能已经将 CXL 视为普通的 System-RAM，这确实存在关键系统进程的内存页（包括代码段和核心数据结构）被分配到 CXL 慢速节点的风险。

针对你的问题，我们可以从“如何规避 PID 1 占用”以及“完全由用户态延迟控制是否可行”两个维度来进行深度技术分析。

---

## 方案一：保持内核自动初始化，但规避 PID 1 占用

在现代 Linux 内核中，即使 CXL 缓存在系统启动时就被自动识别为 `system-ram`，内核和社区也设计了专门的机制来防止核心系统进程“误入”慢速节点。

### 1. 利用 `Movable Zone`（可移动内存域）进行隔离

这是内核默认最核心的防线。当 CXL 驱动在线化（Online）内存时，它通常会将 CXL 内存节点标记为 **`ZONE_MOVABLE`**。

* **原理：** 内核自身的关键数据结构（如页表、进程控制块 Task Struct、内核栈）以及 PID 1 启动时的不可移动内存，**绝对不允许**分配到 `ZONE_MOVABLE` 中。
* **效果：** `systemd` 的核心内核级开销会被强制锁在本地 DDR5 中，只有用户态的匿名内存或页面缓存才能进入 CXL。

### 2. 内核引导参数：`movable_node`

你可以在 GRUB 启动项中加入 `movable_node` 参数。

* **原理：** 该参数通知内核，所有通过热插拔或 CXL 这种无 CPU（CPU-less）节点引入的内存，一律强制划归为 `ZONE_MOVABLE`。这从根本上杜绝了内核核心组件和早期关键守护进程占用 CXL 的可能性。

---

## 方案二：由用户态在启动后手动配置（完全可行）

你的第二种设想——**阻止内核自动 Online，在系统启动完成、核心服务都驻留本地内存后，再由用户手动或脚本激活 CXL**——在工程上是完全可行的，也是目前很多高内聚、低延迟系统选用的激进优化策略。

### 如何实现拦截与延迟激活？

要实现这一功能，核心在于阻止内核在 boot 阶段自动将 CXL 的 DAX 设备转换成 System-RAM。

1. **修改 BIOS 设置或内核参数:** 阻止自动在线化.
在 BIOS 中，如果支持将 CXL 内存的初始状态配置为 `Device-DAX` 模式（或者在内核参数中禁用 CXL 自动热插拔分配），内核在启动时只会为 CXL 建立 `/dev/daxX.Y` 设备文件，而**不会**为其创建 NUMA 节点，更不会分配物理内存页。

或者在内核启动参数（GRUB）中加入：

```text
cxl_core.action_on_error=panic  # 或相关控制参数，确保驱动保持 pass-through 状态

```


2. **等待系统完全启动:** 核心进程驻留 Local.
此时，系统正常引导，`systemd` 及其拉起的所有初始化服务（数据库、网络协议栈、监控等）全部 100% 只能在本地 DDR5 节点（Node 0）上分配内存并完成常驻。


3. **用户态手动或脚本触发转换:** 动态热插拔.
当系统进入稳定运行状态后，通过管理员权限运行 `daxctl` 指令，显式地将指定的 CXL 设备拉线上线：

```bash
sudo daxctl reconfigure-device --mode=system-ram --force dax0.0

```

此时，内核会动态创建一个新的 NUMA 节点（例如 Node 1），并将 CXL 空间作为 `system-ram` 热插拔（Hotplug）进系统伙伴分配器。


---

## 两种方案的技术权衡 (Trade-offs)
##### 方案一：kernel自动，movalbe 隔离：
完全由用户态控制虽然带来了极高的确定性，但也伴随着一些系统级的设计挑战：
优势： 自动化程度高，符合现代层级内存（Tiered Memory）的标准规范。配合 TPP 机制，后续的冷热页迁移（Promotion/Demotion）能够无缝无感知地进行。

劣势： 依赖内核对 ZONE_MOVABLE 的严格划分。若某些第三方用户态核心服务（不属于内核空间）在早期并发启动，仍有微小概率在本地内存瞬时压力大时触及 CXL 边界。

##### 方案二：用户手动完成
优势： 100% 确定性。 在你执行 daxctl 之前，CXL 内存对整个操作系统分配器而言是“不存在”的。你可以确保所有在系统初始化阶段启动的代码、中断处理队列、内核缓冲区都在最快的本地内存中。

劣势： 冷页无法自动降级。 延迟加载意味着系统启动初期的“内存黑洞”。此外，如果在 CXL 在线化之后，系统再次遭遇高负载，新拉起的进程依然会根据 NUMA 分配策略使用 CXL。它更像是一个“静态屏障”，只能保护启动早期的进程。
---

## 总结与工程建议

如果你的核心诉求是**绝对的技术确定性（Deterministic Performance）**，特别是在进行技术专利验证或极限微架构测试时：







## 延后加载cxl memory
要阻止内核在 boot 阶段将 CXL 的 DAX 设备自动转换成 `system-ram`，我们需要先明确这个“自动转换”的动作在 Linux 系统中是由谁触发的。

在现代 Linux 系统中，CXL 内存从硬件被识别到最终变成可用的 `system-ram`，通常会经过以下拦截点：**Udev 规则触发**、**内核 `dax_kmem` 驱动绑定**，以及**内存热插拔（Memory Hotplug）子系统自动上线**。

要切断这个自动流程，我们可以从这三个层面入手。以下是三种主流的实现方法，按推荐程度从高到低排列：

---

### 方法一：屏蔽 Udev 自动化规则（最推荐、最优雅）

在很多现代企业级 Linux 发行版（如 RHEL 9、Ubuntu 22.04+、SUSE）中，安装 `ndctl` 或 `daxctl` 包时，会默认带入一条 udev 规则。这条规则会在内核发现 DAX 设备时，自动在后台静默执行 `daxctl reconfigure-device --mode=system-ram`。

**核心机制：** 只要禁用了这条 udev 规则，设备就会安静地保持在 `/dev/daxX.Y` 状态，等待用户后续的手动指令。

**操作步骤：**

1. 查找系统中是否含有 dax 自动上线的 udev 规则（通常在 `/usr/lib/udev/rules.d/` 或 `/lib/udev/rules.d/` 目录下，文件名为 `90-daxctl-device.rules` 等）。
2. 通过向 `/etc/udev/rules.d/` 创建一个同名的空文件来屏蔽（Mask）它：
```bash
sudo touch /etc/udev/rules.d/90-daxctl-device.rules

```


*(注：`/etc/udev/rules.d/` 目录下的规则优先级高于 `/usr/lib/`，一个空文件会直接覆盖并失效掉系统的默认规则。)*
3. 重新加载 udev 规则并更新 initramfs（如果 udev 规则被打包进了引导镜像）：
```bash
sudo udevadm control --reload
sudo update-initramfs -u  # Ubuntu/Debian
sudo dracut -f            # RHEL/CentOS

```



---

### 方法二：内核参数拦截 `dax_kmem` 驱动（最底层、最彻底）

如果你的系统没有 udev 规则，而是内核自己“过于积极”地将 DAX 挂载为内存，这通常是因为内核的 **`dax_kmem` (DAX Kernel Memory) 驱动**在 boot 阶段自动探测并绑定了该设备。

**核心机制：** `dax_kmem` 的唯一职责就是把 DAX 设备注册进系统的物理内存池。把这个内核模块加入黑名单，就可以彻底斩断内核态的自动转换。

**操作步骤：**

1. 修改 GRUB 引导参数。打开 `/etc/default/grub`。
2. 在 `GRUB_CMDLINE_LINUX` 的末尾追加模块黑名单参数：
```text
modprobe.blacklist=dax_kmem

```


*(或者创建 `/etc/modprobe.d/dax.conf` 写入 `blacklist dax_kmem`)*。
3. 更新 GRUB：
```bash
sudo update-grub  # 或者 grub2-mkconfig -o /boot/grub2/grub.cfg

```



**效果：** 系统启动后，由于 `dax_kmem` 无法加载，CXL 内存将 100% 保持在 `devdax` 字符设备模式。当你在用户态运行 `daxctl reconfigure-device` 时，它会临时加载该模块完成转换。

---

### 方法三：控制内存热插拔（Memory Hotplug）的默认状态

即便 `dax_kmem` 驱动加载了，或者设备被配置成了 `system-ram`，它还需要经过最后一步：**Online（上线）**。只有上线了的内存块，操作系统的伙伴分配器（Buddy Allocator）才能把程序分配进去。

**核心机制：** Linux 的内存热插拔策略默认是 `online`（一发现新内存就立刻上线使用）。我们可以将全局热插拔默认状态改为 `offline`，这样 CXL 内存即使被识别成了 RAM，也会处于“离线/冻结”状态，PID 1 (`systemd`) 绝对无法使用它。

**操作步骤：**

1. 修改 GRUB 引导参数，在 `GRUB_CMDLINE_LINUX` 中添加：
```text
memhp_default_state=offline

```


2. 更新 GRUB 并重启。
3. **启动后的验证与激活：**
系统启动后，你用 `lsmem` 会看到 CXL 的内存块，但状态是 `Offline`。
此时你可以在业务启动前，通过脚本手动将其上线：
```bash
# 临时将策略改为 online
echo online > /sys/devices/system/memory/auto_online_blocks

# 或者针对特定的内存块循环使其 online
for mem in /sys/devices/system/memory/memory*/state; do
    if [ "$(cat $mem)" = "offline" ]; then
        echo online > $mem
    fi
done

```



---

### 技术总结与选型建议

在实际的底层调优和专利环境验证中：

* 如果你是为了**阻止系统自动配置，完全交给用户态控制**，建议使用 **方法一（屏蔽 Udev）** 或 **方法二（黑名单 dax_kmem）**。这会让设备维持在最原始的 `devdax` 状态。
* 建议先用 `udevadm info -a -p /sys/class/dax/dax0.0` 检查一下当前系统中是否有特定的脚本在管理你的 CXL DAX 设备，然后精准切断即可。