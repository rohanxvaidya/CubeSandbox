# emon_data

This directory stores parsed EMON outputs.

Each EMON dataset should use its own subfolder named after the dat basename.

Typical contents per subfolder:
- `<name>.dat`
- `<name>.xlsx`
- `__mpp_*_summary.csv` and/or `__mpp_*_details.csv`
- optional filtered result files
