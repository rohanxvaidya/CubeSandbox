In this file, they are TMA related event;

## Metrics and event

### In the `system view` and `socket view` and `core view` and `thread view`sheet
 - please find out all of `metric_TMA` related event

